name: 'sensor_pulse'
version: '1.0.0'

profile: 'sensor_pulse'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

vars:
  data_dir: "data/raw"

models:
  sensor_pulse:
    staging:
      +materialized: view
    marts:
      +materialized: view